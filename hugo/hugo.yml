title: Daylin's Brain Dump
baseUrl: https://brain.dayl.in/
contentDir: ../slipbox/
enableGitInfo: true

outputs:
  home:
    - HTML
    - json

menu:
  main:
    - name: tags
      url: /tags
      weight: 1
    - name: hubs
      url: /tags/hub
      weight: 2
    - name: search
      url: /search
      weight: 3
    - name: all
      url: /all
      weight: 4
    - name: graph
      url: /graph
      weight: 5

params:
  recentThoughts: true
  # recentThoughtsNum: 10

module:
  imports:
  - path: github.com/daylinmorgan/brain-stem

markup:
  highlight:
    style: catppuccin-mocha
